name: Send Weekly Email Digest

on:
  schedule:
    # Every Sunday at 9:00 AM CDT (14:00 UTC)
    # CDT (summer): UTC-5, so 9 AM CDT = 2 PM UTC (14:00)
    # CST (winter): UTC-6, so 9 AM CST = 3 PM UTC (15:00)
    # Using 14:00 UTC for daylight saving time (March-November)
    - cron: '0 14 * * 0'  # Sunday at 9:00 AM CDT
  workflow_dispatch:  # Allow manual trigger for testing

jobs:
  send-weekly-digest:
    runs-on: ubuntu-latest
    
    steps:
      - name: Display Trigger Time
        run: |
          echo "============================================"
          echo "📅 Weekly Email Digest Workflow Started"
          echo "============================================"
          echo "Triggered at (UTC): $(date -u)"
          echo "Triggered at (CDT): $(TZ='America/Chicago' date)"
          echo "Day of week: $(date +%A)"
          echo "Week number: $(date +%U)"
          echo "============================================"
      
      - name: Send Weekly Email Digest
        run: |
          echo "📤 Calling send-digest API endpoint (weekly trigger)..."
          RESPONSE=$(curl -s -w "\n%{http_code}" -X GET "https://fitness-tracker-flame-kappa.vercel.app/api/send-digest")
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | head -n-1)
          
          echo "HTTP Status Code: $HTTP_CODE"
          echo "Response: $BODY"
          
          if [ "$HTTP_CODE" -eq 200 ]; then
            echo "✅ Weekly email digest sent successfully!"
            echo "📝 Note: Currently using daily digest endpoint (last 24 hours)"
            echo "💡 To send 7-day summary, create /api/send-weekly-digest endpoint"
          else
            echo "❌ Failed to send weekly digest. HTTP $HTTP_CODE"
            exit 1
          fi
      
      - name: Verify Completion
        run: |
          echo "============================================"
          echo "✅ Weekly Email Digest Workflow Completed"
          echo "Completed at (UTC): $(date -u)"
          echo "Completed at (CDT): $(TZ='America/Chicago' date)"
          echo "Next run: Next Sunday at 9:00 AM CDT"
          echo "============================================"
